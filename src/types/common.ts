export type PositionType = {
    x: number,
    y: number
};

export type SizeType = {
    width: number,
    height: number
};

export type DirectionType = "UP" | "DOWN" | "LEFT" | "RIGHT" | undefined;
